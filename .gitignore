# Build output — regenerated by `npm run build` on every Netlify deploy
dist/

# Node / Netlify local state
node_modules/
.netlify/
.env
.env.*

# Scratch / working / superseded source files — never shipped, never committed
data1.js
rjecnik1.js
app1.html
__kartice-tmp.html
*.bak*
*.jsonl
*.ps1
*.txt
*.csv
