
  /* ================= PROGRESS ================= */
  .progTabovi { display: flex; gap: 0.25rem; background: var(--tiho); padding: 0.25rem;
    border-radius: var(--rad-m); margin-bottom: 1.5rem; width: fit-content; max-width: 100%; flex-wrap: wrap; }
  .progTabovi button { border: 0; background: transparent; font-family: var(--naslov); font-weight: 600;
    font-size: var(--t-m); color: var(--muted); padding: 0.5rem 1rem; border-radius: var(--rad-s); cursor: pointer; }
  .progTabovi button.aktivan { background: var(--card); color: var(--ink); box-shadow: 0 1px 2px rgba(25,23,19,.08); }
  .progTabovi button .br { font-size: var(--t-xs); font-weight: 500; opacity: .7; }

  .progNas { font-family: var(--naslov); font-size: var(--t-xl); font-weight: 600;
    margin: 2rem 0 0.25rem; letter-spacing: -0.01em; }
  .progOpis { color: var(--muted); font-size: var(--t-s); margin: 0 0 0.875rem; max-width: 46rem; }
  .progPloca { background: var(--card); border: 1px solid var(--rub); border-radius: var(--rad-l);
    padding: 1rem 1.125rem; }

  .progValute { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.75rem; margin-bottom: 0.75rem; }
  .progValK { background: var(--card); border: 1px solid var(--rub); border-radius: var(--rad-l);
    padding: 0.875rem 1rem; position: relative; overflow: hidden; }
  .progValK::before { content: ""; position: absolute; left: 0; top: 0; bottom: 0; width: 3px; background: var(--boja); }
  .progValK .imeV { font-size: var(--t-xs); color: var(--muted); text-transform: uppercase;
    letter-spacing: .06em; font-weight: 600; }
  .progValK .broj { font-family: var(--naslov); font-size: var(--t-4xl); font-weight: 700;
    color: var(--boja); line-height: 1.05; margin: 0.125rem 0 0.375rem; }
  .progStaza { height: 5px; background: var(--tiho); border-radius: var(--rad-xs); overflow: hidden; }
  .progStaza i { display: block; height: 100%; width: 0; background: var(--boja);
    border-radius: var(--rad-xs); transition: width 1s cubic-bezier(.2,.7,.3,1); }
  .progValK .doKud { font-size: var(--t-xs); color: var(--muted); margin-top: 0.375rem; }
  .progValK .doKud b { color: var(--ink); font-weight: 700; }
  .progValK.pun .doKud { color: var(--zelena); }

  .progStat { display: grid; grid-template-columns: repeat(4, 1fr); gap: 0.75rem; }
  .progStatK { background: var(--papir); border: 1px solid var(--rub); border-radius: var(--rad-m);
    padding: 0.625rem 0.75rem; }
  .progStatK .b { font-family: var(--naslov); font-size: var(--t-2xl); font-weight: 700; line-height: 1.1; }
  .progStatK .b small { font-size: var(--t-m); color: var(--muted); font-weight: 500; }
  .progStatK .l { font-size: var(--t-xs); color: var(--muted); }

  .progMrezaOkvir { overflow-x: auto; padding-bottom: 0.25rem; }
  .progMreza { display: grid; grid-template-columns: 2.5rem repeat(5, minmax(5.5rem, 1fr));
    gap: 0.3125rem; min-width: 34rem; }
  .progGlava { font-size: var(--t-xs); font-weight: 700; text-transform: uppercase; letter-spacing: .06em;
    color: var(--muted); padding: 0 0 0.25rem 0.125rem; }
  .progRazina { display: flex; align-items: center; justify-content: flex-end; padding-right: 0.375rem;
    font-size: var(--t-xs); color: var(--muted); font-variant-numeric: tabular-nums; }
  .progCelija { position: relative; border: 1px solid var(--rub); border-radius: var(--rad-m);
    background: var(--card); padding: 0.3125rem 0.4375rem 0.375rem; cursor: pointer; text-align: left;
    font: inherit; color: inherit; min-height: 2.5rem; display: flex; flex-direction: column;
    justify-content: space-between; transition: transform .12s, box-shadow .12s, border-color .12s; }
  .progCelija:hover { transform: translateY(-1px); box-shadow: 0 3px 10px rgba(25,23,19,.09); border-color: var(--boja); }
  .progCelija .pct { font-family: var(--naslov); font-size: var(--t-s); font-weight: 700;
    font-variant-numeric: tabular-nums; }
  .progCelija .odn { font-size: var(--t-2xs); color: var(--muted); font-variant-numeric: tabular-nums; }
  .progCelija .progStaza { height: 3px; margin-top: 0.25rem; }
  .progCelija.nula .pct { color: var(--muted); font-weight: 600; }
  .progCelija.puna { background: color-mix(in srgb, var(--boja) 9%, var(--card));
    border-color: color-mix(in srgb, var(--boja) 35%, var(--rub)); }
  .progCelija.puna .pct { color: var(--boja); }
  .progCelija.puna .kvaka { position: absolute; top: 0.3125rem; right: 0.4375rem; color: var(--boja);
    font-size: var(--t-s); line-height: 1; }
  .progCelija.zakljucana { background: var(--tiho); border-style: dashed; cursor: help; }
  .progCelija.zakljucana .pct { color: var(--muted); font-size: var(--t-2xs); font-weight: 600;
    text-transform: uppercase; letter-spacing: .05em; }
  .progCelija.odabrana { border-color: var(--boja);
    box-shadow: 0 0 0 2px color-mix(in srgb, var(--boja) 25%, transparent); }
  .progCelija.prazno { border: 0; background: transparent; cursor: default; min-height: 0; }
  .progCelija.prazno:hover { transform: none; box-shadow: none; }

  .progLegenda { display: flex; flex-wrap: wrap; gap: 0.875rem; margin-top: 0.75rem;
    font-size: var(--t-xs); color: var(--muted); }
  .progLegenda span { display: inline-flex; align-items: center; gap: 0.375rem; }
  .progLegenda i { width: 0.875rem; height: 0.875rem; border-radius: 3px; border: 1px solid var(--rub);
    background: var(--card); display: inline-block; }
  .progLegenda i.pun { background: color-mix(in srgb, var(--plavaVok) 25%, var(--card)); border-color: var(--plavaVok); }
  .progLegenda i.pola { background: linear-gradient(90deg,
    color-mix(in srgb, var(--plavaVok) 45%, var(--card)) 50%, var(--card) 50%); }
  .progLegenda i.zak { background: var(--tiho); border-style: dashed; }

  .progLadica { margin-top: 0.875rem; background: var(--card); border: 1px solid var(--rub);
    border-radius: var(--rad-l); overflow: hidden; animation: progOtvori .28s cubic-bezier(.2,.7,.3,1); }
  @keyframes progOtvori { from { opacity: 0; transform: translateY(-6px); } to { opacity: 1; transform: none; } }
  .progLadicaGlava { display: flex; align-items: baseline; gap: 0.75rem; flex-wrap: wrap;
    padding: 0.875rem 1.125rem; border-bottom: 1px solid var(--rub); background: var(--papir); }
  .progLadicaGlava h3 { font-family: var(--naslov); font-size: var(--t-xl); font-weight: 700;
    margin: 0; letter-spacing: -0.01em; }
  .progLadicaGlava .znak { font-size: var(--t-xs); font-weight: 700; text-transform: uppercase;
    letter-spacing: .06em; color: var(--boja); }
  .progLadicaGlava .rez { margin-left: auto; font-family: var(--naslov); font-weight: 700;
    font-variant-numeric: tabular-nums; }
  .progLadicaGlava .rez small { color: var(--muted); font-weight: 500; }
  .progLadicaGlava .x { border: 0; background: transparent; color: var(--muted); font-size: var(--t-xl);
    cursor: pointer; line-height: 1; padding: 0 0.25rem; font-family: inherit; }
  .progVjezbe { list-style: none; margin: 0; padding: 0.375rem; }
  .progVjezbe li { display: grid; grid-template-columns: 1.75rem 1fr 5.5rem 6rem; align-items: center;
    gap: 0.5rem; padding: 0.5rem 0.75rem; border-radius: var(--rad-m); cursor: pointer; }
  .progVjezbe li:hover { background: var(--tiho); }
  .progVjezbe .br { font-size: var(--t-xs); color: var(--muted); font-variant-numeric: tabular-nums; }
  .progVjezbe .ime { font-size: var(--t-m); min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .progVjezbe .pts { text-align: right; font-family: var(--naslov); font-size: var(--t-m);
    font-variant-numeric: tabular-nums; white-space: nowrap; }
  .progVjezbe .pts small { color: var(--muted); }
  .progVjezbe li.pun .pts b { color: var(--boja); }
  .progVjezbe li.nula .ime, .progVjezbe li.nula .pts { color: var(--muted); }
  .progOznaka { font-size: var(--t-2xs); text-transform: uppercase; letter-spacing: .05em;
    background: var(--tiho); color: var(--muted); border-radius: var(--rad-xs);
    padding: 0.0625rem 0.375rem; margin-left: 0.5rem; }
  .progLadicaPod { padding: 0.75rem 1.125rem 1rem; border-top: 1px solid var(--rub); background: var(--papir);
    font-size: var(--t-s); color: var(--muted); display: flex; gap: 0.75rem; align-items: center; flex-wrap: wrap; }
  .progLadicaPod b { color: var(--ink); }

  .progPopravi { list-style: none; margin: 0; padding: 0; display: grid; gap: 0.5rem; }
  .progPopravi li { display: grid; grid-template-columns: 8.5rem 1fr auto; gap: 0.75rem; align-items: center;
    background: var(--card); border: 1px solid var(--rub); border-radius: var(--rad-m);
    padding: 0.625rem 0.875rem; cursor: pointer; transition: border-color .12s, transform .12s; }
  .progPopravi li:hover { border-color: var(--boja); transform: translateX(2px); }
  .progPopravi .gdje { font-size: var(--t-xs); font-weight: 700; color: var(--boja);
    text-transform: uppercase; letter-spacing: .04em; }
  .progPopravi .sto { font-size: var(--t-m); min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .progPopravi .sto small { color: var(--muted); display: block; font-size: var(--t-xs); }
  .progPopravi .koliko { text-align: right; font-family: var(--naslov); font-weight: 700; white-space: nowrap; }
  .progPopravi .koliko small { display: block; color: var(--muted); font-weight: 500; font-size: var(--t-xs); }

  .progTockice { display: flex; flex-wrap: wrap; gap: 4px; }
  .progTockice i { width: 0.875rem; height: 0.875rem; border-radius: 3px; background: var(--tiho);
    border: 1px solid var(--rub); display: block; cursor: pointer; }
  .progTockice i.d { background: color-mix(in srgb, var(--zelena) 55%, var(--card)); border-color: var(--zelena); }
  .progTockice i.p { background: color-mix(in srgb, var(--zlatna) 40%, var(--card)); border-color: var(--zlatna); }

  .progMini { display: grid; grid-template-columns: repeat(2, 1fr); gap: 0.5rem; }
  .progMiniR { display: grid; grid-template-columns: 1.5rem 1fr 4.5rem; gap: 0.625rem; align-items: center;
    background: var(--card); border: 1px solid var(--rub); border-radius: var(--rad-m);
    padding: 0.5rem 0.75rem; cursor: pointer; transition: border-color .12s, transform .12s; }
  .progMiniR:hover { border-color: var(--plava); transform: translateX(2px); }
  .progMiniR .e { font-size: var(--t-l); }
  .progMiniR .n { font-size: var(--t-m); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .progMiniR .n small { display: block; font-size: var(--t-xs); color: var(--muted); }
  .progMiniR .p { text-align: right; font-family: var(--naslov); font-weight: 700; font-size: var(--t-m);
    font-variant-numeric: tabular-nums; }
  .progMiniR .p small { color: var(--muted); font-weight: 500; }
  .progMiniR.nula .n, .progMiniR.nula .p { color: var(--muted); }
  .progMiniR.pun .p { color: var(--zelena); }

  .progLjudi { width: 100%; border-collapse: collapse; background: var(--card);
    border: 1px solid var(--rub); border-radius: var(--rad-l); overflow: hidden; }
  .progLjudi th { text-align: right; font-size: var(--t-xs); text-transform: uppercase; letter-spacing: .05em;
    color: var(--muted); font-weight: 700; padding: 0.625rem 0.75rem; border-bottom: 1px solid var(--rub);
    background: var(--papir); white-space: nowrap; }
  .progLjudi th:first-child, .progLjudi td:first-child { text-align: left; }
  .progLjudi th:nth-last-child(-n+2), .progLjudi td:nth-last-child(-n+2) { text-align: left; }
  .progLjudi td { padding: 0.625rem 0.75rem; border-bottom: 1px solid var(--rub); font-size: var(--t-m);
    text-align: right; font-variant-numeric: tabular-nums; white-space: nowrap; }
  .progLjudi tr:last-child td { border-bottom: 0; }
  .progLjudi tr.ja td { background: color-mix(in srgb, var(--plava) 6%, var(--card)); }
  .progLjudi .imeC { font-weight: 600; display: flex; align-items: center; gap: 0.5rem; flex-wrap: wrap; }
  .progLjudi .idC { font-family: ui-monospace, Consolas, monospace; font-size: var(--t-xs);
    color: var(--muted); background: var(--tiho); border-radius: var(--rad-xs);
    padding: 0.0625rem 0.375rem; font-weight: 500; }
  .progLjudi tbody tr.klik { cursor: pointer; }
  .progLjudi tbody tr.klik:hover td { background: var(--tiho); }
  .progZnacka { font-size: var(--t-2xs); text-transform: uppercase; letter-spacing: .05em;
    border-radius: var(--rad-xs); padding: 0.125rem 0.375rem; font-weight: 700; }
  .progZnacka.otv { background: color-mix(in srgb, var(--zelena) 14%, var(--card)); color: var(--zelena); }
  .progZnacka.zat { background: var(--tiho); color: var(--muted); }

  .progKod { font-family: ui-monospace, Consolas, monospace; font-size: var(--t-3xl); font-weight: 700;
    letter-spacing: .16em; background: var(--tiho); border: 1px dashed var(--rub);
    border-radius: var(--rad-m); padding: 0.375rem 0.875rem; }
  .progRed { display: flex; align-items: center; gap: 0.75rem; flex-wrap: wrap; }
  .progUnos { font-family: ui-monospace, Consolas, monospace; font-size: var(--t-l); letter-spacing: .12em;
    text-transform: uppercase; width: 8.5rem; padding: 0.5rem 0.75rem; border: 1px solid var(--rub);
    border-radius: var(--rad-m); background: var(--card); color: var(--ink); }
  .progIme { font-family: inherit; font-size: var(--t-l); width: 14rem; padding: 0.5rem 0.75rem;
    border: 1px solid var(--rub); border-radius: var(--rad-m); background: var(--card); color: var(--ink); }
  .progIzbor { display: flex; gap: 0.625rem; align-items: flex-start; background: var(--card);
    border: 1px solid var(--rub); border-radius: var(--rad-m); padding: 0.75rem 0.875rem;
    cursor: pointer; flex: 1 1 15rem; }
  .progIzbor.aktivan { border-color: var(--plava); box-shadow: 0 0 0 2px color-mix(in srgb, var(--plava) 18%, transparent); }
  .progIzbor .t { font-size: var(--t-m); font-weight: 600; display: block; }
  .progIzbor .o { font-size: var(--t-xs); color: var(--muted); }
  .progIzbor input { margin-top: 0.3125rem; accent-color: var(--plava); }
  .progSitni { font-size: var(--t-s); padding: 0.3125rem 0.625rem; }
  .progTiho { font-size: var(--t-s); color: var(--muted); }

  header .valute.klik { cursor: pointer; }
  header .valute.klik:hover { background: var(--rub); }

  @media (max-width: 860px) {
    .progValute, .progStat { grid-template-columns: repeat(2, 1fr); }
    .progMini { grid-template-columns: 1fr; }
    .progVjezbe li { grid-template-columns: 1.5rem 1fr 4.75rem; }
    .progVjezbe .sbar { display: none; }
    .progPopravi li { grid-template-columns: 1fr auto; }
    .progPopravi .gdje { grid-column: 1 / -1; }
  }
