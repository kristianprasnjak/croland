# Copy to .env (gitignored, never committed) for local `netlify dev` testing.
# Netlify Functions read these via process.env; netlify dev loads .env automatically.

SUPABASE_URL=https://krunohdgohuebmafepmb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_ID=price_...
# From `stripe listen --forward-to localhost:8888/.netlify/functions/stripe-webhook`
STRIPE_WEBHOOK_SECRET=whsec_...
# Optional — defaults to the request Origin header if unset
SITE_URL=http://localhost:8888
